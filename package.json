{
  "name": "function-translate",
  "version": "1.0.0",
  "author": "wone",
  "description": "tanslate the math-function to programming language(javascript) expression",
  "main": "dist/funcTrans.min.js",
  "license": "MIT",
  "files": [
    "src",
    "dist/*.js"
  ],
  "keywords": [
    "complie function expression"
  ],
  "scripts": {
    "test": "jest",
    "build": "yarn test && rollup --config",
    "pu": "yarn test && yarn build && yarn publish",
    "sync:github": "git add -A && git commit && git push -u origin master"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.14.5",
    "jest": "^27.0.4",
    "rollup": "^2.52.1",
    "rollup-plugin-terser": "^7.0.2"
  }
}
